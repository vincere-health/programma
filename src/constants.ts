export enum JobStates {
  CREATED = 'created',
  ACTIVE = 'active',
  READY = 'ready',
  PROCESSING = 'processing',
  FAILED = 'failed',
  COMPLETED = 'completed',
  CANCELLED = 'cancelled',
}


export enum EventStates {
  ERROR = 'error',
}